// Bump this on every commit. Format: major.minor
// major = breaking changes or large feature batches
// minor = each individual commit after the last major
// History: v0.1–v0.8 = foundation, v1.0 = big batch, v1.1+ = refinements
export const VERSION = '1.29';
